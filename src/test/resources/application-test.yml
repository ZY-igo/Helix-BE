spring:
  datasource:
    url: jdbc:postgresql://192.168.115.23:5432/helix
    driver-class-name: org.postgresql.Driver
    username: admin
    password: Sipc@postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  encryption:
    master-key: test-master-key-for-unit-tests-only

rocketmq:
  enabled: false
  name-server: localhost:9876
  producer:
    group: test-producer-group
  consumer:
    group: test-consumer-group

logging:
  level:
    com.sipc115.helix: DEBUG
